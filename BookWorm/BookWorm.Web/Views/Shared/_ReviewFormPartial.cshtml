@model ReviewFormViewModel

@*<div class="row">
    <div class="col-8 offset-2">
        <form method="post">
            <div class="mb-3">
                <label asp-for="@Model.Title" class="form-label">Title</label>
                <input asp-for="@Model.Title" class="form-control" />
                <span asp-validation-for="@Model.Title"></span>
            </div>

            <div class="mb-3">
                <div class="mb-1">
                    <label asp-for="@Model.Description" class="form-label">Description</label>
                    <textarea asp-for="@Model.Description" class="form-control" rows="5"></textarea>
                    <span asp-validation-for="@Model.Description"></span>
                </div>
            </div>

            <div class="mb-3">
                <label asp-for="@Model.Content" class="form-label">Content</label>
                <textarea asp-for="@Model.Content" class="form-control" rows="5"></textarea>
                <span asp-validation-for="@Model.Content"></span>
            </div>

            <div class="mb-3">
                <label asp-for="@Model.IsPrivate" class="form-check-label">Private</label>
                <input class="form-check-input" asp-for="@Model.IsPrivate" />
            </div>

            <div class="mb-3">
                <label asp-for="@Model.CategoryId" class="form-label"></label>
                <select asp-for="@Model.CategoryId" class="form-select" aria-label="Default select example">
                    @foreach (var category in Model.Categories)
                    {
                        <option value="@category.Id">@category.Name</option>
                    }
                </select>
            </div>

            <div class="text-center">
                <input class="btn btn-primary mt-3" type="submit" value="@ViewData["Action"]" />
            </div>
        </form>
    </div>
</div>*@